\paragraph{From short-horizon forecasting to route generation.}
Urban mobility traces record how people realize intent through space: not only \emph{where} they go, but \emph{how} they get there.
Recent progress in sequence models and diffusion models \citep{vaswani2017attention,ho2020ddpm} has made short-horizon trajectory prediction increasingly accurate.
Yet many practical and scientific tasks require a different capability: \emph{route generation} over a full trip.
Given an origin--destination intent and context (e.g., departure time and neighborhood semantics), a route generator must sample an entire path that is realistic, feasible, and diverse.

\paragraph{Why long-horizon route generation fails.}
We observe two recurrent failure modes when extending generative modeling from seconds to full trips.
First, intent can drift: purely autoregressive rollouts accumulate small errors and become globally inconsistent with the destination.
Second, feasibility can break: when emphasizing diversity (e.g., via latent-variable models \citep{kingma2014auto} or standard diffusion), generators often hallucinate physically invalid paths that cut through obstacles or deviate systematically from the road network---especially when explicit map supervision is absent or unreliable.

\paragraph{Root cause: topological multi-modality and destructive averaging.}
The core difficulty is \emph{topological multi-modality}.
Even for the same origin and destination, multiple corridor-level choices can be equally reasonable.
End-to-end coordinate generators trained with likelihood or Euclidean losses therefore face a destructive ``averaging'' pressure: the mean of several feasible routes is often an infeasible straight line.
More importantly, this multi-modality is \emph{structured}: corridor-level alternatives are discrete and topological, while variations \emph{within} a corridor are comparatively continuous.
Representing this structure only implicitly in a continuous coordinate output space leads to brittle long-horizon behavior: samples can collapse to destructive averages or drift off-road and become destination-inconsistent.
This suggests that route generation should be modeled as a \emph{decision--execution} process rather than a single homogeneous sequence problem, with an explicit \emph{corridor commitment} variable that separates discrete corridor choice from continuous geometric realization.

\paragraph{A diagnostic baseline: averaging vs.\ blur in continuous output space.}
We first provide a diagnostic experiment (Fig.~\ref{fig:mode-collapse}) using end-to-end baselines trained under the same data and conditioning.
To avoid cherry-picking, we fix diagnostic OD cases via a ground-truth corridor audit (Sec.~\ref{sec:experiments}).
We observe two characteristic symptoms of topological multi-modality: (i) deterministic coordinate regression collapses to overly direct ``average'' routes, and (ii) stochastic generation can avoid hard collapse yet still become destination-inconsistent and diverge off-road.
Together, these failures suggest that the bottleneck is not simply model capacity, but \emph{mode interference} when corridor-level decisions are represented only implicitly in a continuous coordinate output space.
Critically, our ablations show that corridor diversity must be (i) created as an explicit \emph{topological commitment} at the decision stage and (ii) \emph{preserved} during execution: a strong continuous refiner can overwrite corridor commitments, while a commitment-preserving refinement maintains coverage.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/fig_mode_collapse.pdf}
  \caption{Corridor-level multi-modality in route generation. (a) Ground truth routes for a fixed OD pair show two distinct corridor alternatives. (b) L2 regression collapses to a destructive average. (c) End-to-end diffusion avoids hard collapse but exhibits destination inconsistency and off-road divergence in long-horizon generation. (d) CascadeTraj preserves corridor commitments and covers multiple corridor alternatives through explicit decision--execution decomposition.}
  \label{fig:mode-collapse}
\end{figure}

\paragraph{CascadeTraj: hierarchical route generation via corridor commitment.}
We propose \textbf{CascadeTraj}, a hierarchical generative framework for route generation.
At the \emph{decision} stage, the model samples a sparse route skeleton (e.g., waypoints) that represents discrete corridor-level alternatives.
At the \emph{execution} stage, the model generates a full-resolution continuous route conditioned on the skeleton, capturing local geometry and motion patterns.
This decomposition turns a brittle one-shot generation problem into two conditional problems with clearer responsibilities: (i) committing to a coherent topological plan, and (ii) executing a physically plausible realization.

\paragraph{Soft priors and optional macro statistics.}
To encourage feasibility without collapsing support, we incorporate map information as \emph{soft} priors (e.g., road-proximity fields) rather than hard output masking.
We treat inference-observable environment semantics (POI/land-use/imagery) and population-level macro statistics (e.g., census covariates) strictly as optional ablations for extensibility; empirically, richer semantics provides limited benefit for corridor commitment in our setting.
The paper's focus remains the generative architecture for the \emph{route itself}, not population synthesis.

\paragraph{Contributions.}
\begin{itemize}
  \item \textbf{Finding 1 (Diagnosis):} we show that corridor-level multi-modality causes structural failures for end-to-end coordinate generators: deterministic regression collapses to destructive averages, while stochastic generation can become destination-inconsistent and diverge off-road in long-horizon generation.
  \item \textbf{Finding 2 (Mechanism):} we show that a decision--execution decomposition with an explicit waypoint commitment recovers corridor diversity, and that composition must be commitment-preserving: naive cascades can overwrite corridor commitments, while weak residual refinement maintains coverage.
  \item \textbf{Design principle (Stage-specific data utility):} we find that heterogeneous urban data must be placed at the right stage: injecting map priors into corridor commitment can reduce diversity, while applying soft road priors at the execution stage substantially improves feasibility (on-road rate) without degrading micro accuracy.
\end{itemize}
