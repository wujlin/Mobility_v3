\paragraph{Data.}
Placeholder: specify datasets (e.g., WorldTrace city subsets), preprocessing, and train/val/test splits.

\paragraph{Baselines.}
Placeholder: include end-to-end sequence models, end-to-end diffusion, and map-constrained variants (as diagnostics).

\paragraph{Metrics.}
Placeholder: intent consistency (arrival / endpoint error), realism (path similarity / shape metrics), feasibility proxies (on-road ratio with sensitivity), and diversity (coverage / distinct corridors).

\paragraph{Ablations.}
Placeholder: waypoint hierarchy, soft map priors, semantic channels (POI/imagery), and optional census guidance.

\paragraph{Sanity checks and failure analysis.}
Placeholder: report common degeneracies (overly direct routes, corridor collapse, off-road artifacts) and how each design choice addresses a specific failure mode.
